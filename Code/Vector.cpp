#include "Vector.h"
/*
Vector::Vector(int column)
    : Matrix(1,column)
{
}

Vector::Vector(const Matrix &ref)
    : Matrix(1,ref.column)
{
    if (ref.row != 1 || ref.row != column)
    {
        printf("Error : Vector_Vector(Matrix)");
        exit(-1);
    }

    int i;
    for(i=0;i<column;i++)
        elem[0][i] = ref.elem[0][i];
}

Vector::~Vector()
{
    ~Matrix();
}

void Vector::SetElem(int column,double value)
{
    Matrix.SetElem(0,column,value);
}
*/
